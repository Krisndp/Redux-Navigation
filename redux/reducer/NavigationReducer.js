const defaultState = {value: 1, name: 'asd'};
const NavigationRedux = (state = defaultState, action)=>{
    switch(action.type){
        default:
        return state;
    }
}

export default NavigationRedux;